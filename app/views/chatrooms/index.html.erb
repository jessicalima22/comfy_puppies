<div class="container padding_navbar">
  <h1>chatrooms</h1>
  <ul>
    <% @chatrooms.each do |chatroom| %>
      <li><%= link_to chatroom.name, animal_chatroom_path(@animal, chatroom) %></li>
    <% end %>
  </ul>

  <%= link_to 'New Chatroom', new_animal_chatroom_path(@animal), class: "btn btn-secondary" %>
</div>
